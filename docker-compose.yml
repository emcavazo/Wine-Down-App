services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: wine
      POSTGRES_PASSWORD: winepass
      POSTGRES_DB: winedown
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data  # persists data between restarts
volumes:
  pgdata: